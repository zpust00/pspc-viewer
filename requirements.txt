# PSPC Viewer — Python dependencies
# Install with:  pip install -r requirements.txt

# Runtime
pandas>=2.0
openpyxl>=3.1
# tkinter is built into Python — no separate install needed

# Build (only needed to create .exe)
# pyinstaller>=6.0
